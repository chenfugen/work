
const state = {
    fileLoad: '/lexy/api2/common/file?token=' + localStorage.getItem('token') + '&fileName='
}

const getters = {
}

const mutations = {
}

export default { state, getters, mutations }
